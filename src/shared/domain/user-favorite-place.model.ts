export type UserFavoritePlace = {
  id: string
  placeId: string
  name: string
  avatar: string
  createdAt: string
}

export type CreatedUserFavoritePlace = {
  id: string
  userId: string
  placeId: string
  createdAt: string
}
